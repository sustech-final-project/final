package game.minesweeper.checkerboard;

import java.io.*;

public class checkerboard {
    /**************************************************
    本程序用于棋盘生成后的检查，若能继续游戏，返回值为true，若不能，返回值为false。
     其中，使用为ischeckerboardlegal(a,b);
     a,b,c,d均为正整数，a为行数，b为列数
     判断要求如下：
        1.避免九宫格内均是雷；


     *************************************************/
    public boolean ischeckerboardlegal(int row, int column){

        return true;
    }
}
